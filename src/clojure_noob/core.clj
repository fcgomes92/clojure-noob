(ns clojure-noob.core
  (:require [clojure-noob.ch3 :as ch3]
            [clojure-noob.ch4 :as ch4])
  (:gen-class))

(require
 '[clojure-noob.fwpd :as fwpd])
(require
 '[clojure-noob.ch5 :as ch5])

(defn -main
  "I don't> do a whole lot ... yet."
  [& args]
  (println "Hello"))